deploy:
  target_branch: master
  local_dir: ./public
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: src

notifications:
  email:
    on_failure: always

branches:
  only:
    - src
